<!DOCTYPE html>
    <html>
    <head>
        <meta charset="UTF-8">
        <title></title>

    </head>
    <body>
   <h1>All Pins </h1>
   <a href="{{ path('pins_create') }}">Create Pin</a>

    {% for pin in pins %}
        <article>
            <h1>{{ pin.title }}</h1>
            <p>{{ pin.description }}</p>
        </article>
    {% else %}
        <p>Sorry, no pins yet!</p>
    {% endfor %}


    </body>
    </html>